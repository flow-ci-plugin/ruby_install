
# ruby_install Step
Install dependencies use `bundle install`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: ruby_install
    enable: true
    failure: true
    plugin:
      name: ruby_install
      inputs:
```
